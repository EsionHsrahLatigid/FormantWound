# FormantWound Session Timeline

## 2026-08-16

- Reviewed the FormantWound expansion brief and the EHL shared skills for JUCE, EHL identity/design, and Obsidian workflow.
- Checked `../ehl` for durable session evidence. No standalone session-log bundle was present in the discovered evidence paths; the durable references available there are the design rules, the design-skill repo, and web/git history.
- Confirmed the public-copy constraint: the private descriptor stays internal and must not appear on public surfaces, repository descriptions, README text, UI, plugin metadata, release text, or web copy.
- Surveyed the source-filter / formant literature stack: Rabiner formant synthesis, source-filter control, Bark-space vowel interpolation, and cepstral analysis.
- Locked the working identity as `FormantWound` / `formantwound` / `jp.ehl.formantwound` / `FmWt`.
- Obsidian HTTP connectivity was not available in this environment, so the durable evidence for this note set is kept in the workspace and in local git history for the sibling EHL repositories.
